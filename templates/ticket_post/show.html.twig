{% extends 'base.html.twig' %}

{% block title %}TicketPost{% endblock %}

{% block body %}
    <h1>TicketPost</h1>

    <table class="table">
        <tbody>

        {# {% for message in message_post %} #}

            <div>
                <h3>{{ticket_post.title}}</h3>
                <p>{{ ticket_post.createdAt ? ticket_post.createdAt|date('d-m-Y à H:i') : '' }}</p>
                {# <p>{{ticket_post.}}</p> #}
            </div>
        {# {% end %}         #}
        </tbody>
    </table>

    <a href="{{ path('ticket_post_index') }}">back to list</a>

    <a href="{{ path('ticket_post_edit', {'id': ticket_post.id}) }}">edit</a>

    {{ include('ticket_post/_delete_form.html.twig') }}
{% endblock %}
