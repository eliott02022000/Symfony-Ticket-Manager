{% extends 'base.html.twig' %}

{% block title %}MessagePost{% endblock %}

{% block body %}
    <h1>MessagePost</h1>

    <table class="table">
        <tbody>
            <div>
                <h3>{{message_post.MessageField}}</h3>
                <p>{{ message_post.createdAt ? message_post.createdAt|date('d-m-Y à H:i') : '' }}</p>
            </div>
        </tbody>
    </table>

    <a href="{{ path('message_post_index') }}">back to list</a>

    <a href="{{ path('message_post_edit', {'id': message_post.id}) }}">edit</a>

    {{ include('message_post/_delete_form.html.twig') }}
{% endblock %}
